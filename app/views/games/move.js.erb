<%= render :partial => 'show_messages' %>
$('.turn_menu').html('<%= escape_javascript render :partial => 'turn_menu' %>');

//TODO move stuff
<% if @path %>
  <% @path.each_with_index do |coord,index| %>
    setTimeout(
      function(){
        // debugging line
        //show_message("<%= coord.inspect %>");
        move("#character_"+<%= @game.current_character_id %>,<%= raw coord.inspect %>);
      }, 
      <%=index * 150%>
    );
  <% end %>
<% end %>
<%= @game.current_character.current_turn_js_call%>
